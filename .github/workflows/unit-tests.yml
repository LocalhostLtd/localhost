name: Tests

# edit this file later for testing

on:
  workflow_call:
    inputs:
      run-frontend-tests:
        description: 'Run frontend tests'
        required: true
        default: 'true'
      run-backend-tests:
        description: 'Run backend tests'
        required: true
        default: 'true'

jobs:
  unit-tests-frontend:
    if: ${{ inputs.run-frontend-tests == 'true' }}
      name: Run unit tests frontend
      runs-on: self-hosted
      # defaults:
      #   run:
      #     working-directory: ./frontend

      steps:


        - name: Mock Test
          run: echo 'Unit Tests for Frontend go here'

        # - name: Check out Git repository
        #   uses: actions/checkout@v2

        # - uses: actions/cache@v2
        #   with:
        #     path: ~/.npm
        #     key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        #     restore-keys: |
        #       ${{ runner.os }}-node-

        # - name: Set up Node.js
        #   uses: actions/setup-node@v1
        #   with:
        #     node-version: 20

        # # Installs dev dependencies
        # - name: Install dev dependencies
        #   run: npm install

        # - name: Run build
        #   run: npm run build --if-present

        # - name: Run tests
        #   run: npm test

  unit-tests-backend:
    if: ${{ inputs.run-backend-tests == 'true' }}
      name: Run unit tests backend
      runs-on: self-hosted
      # defaults:
      #   run:
      #     working-directory: ./frontend

      steps:
        - name: Mock Test
          run: echo 'Unit Tests for Backend go here'

        # - name: Check out Git repository
        #   uses: actions/checkout@v2

        # - uses: actions/cache@v2
        #   with:
        #     path: ~/.npm
        #     key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        #     restore-keys: |
        #       ${{ runner.os }}-node-

        # - name: Set up Node.js
        #   uses: actions/setup-node@v1
        #   with:
        #     node-version: 20

        # # Installs dev dependencies
        # - name: Install dev dependencies
        #   run: npm install

        # - name: Run build
        #   run: npm run build --if-present

        # - name: Run tests
        #   run: npm test
